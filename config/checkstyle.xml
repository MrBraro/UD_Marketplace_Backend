<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
    Configuración mínima de Checkstyle para UD Marketplace Backend.
    Solo verifica problemas que rompen calidad sin imponer restricciones
    de nomenclatura (el proyecto usa nombres en español, CLAUDE.md §1).

    Autor: Daniel Perez
    Versión: 1.0
-->
<module name="Checker">

    <property name="charset"        value="UTF-8"/>
    <property name="severity"       value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="TreeWalker">

        <!-- Importaciones duplicadas o innecesarias -->
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>

        <!-- Expresiones booleanas simplificables -->
        <module name="SimplifyBooleanExpression"/>
        <module name="SimplifyBooleanReturn"/>

        <!-- Sentencias vacías (punto y coma suelto) -->
        <module name="EmptyStatement"/>

        <!-- Comparación de strings con == en lugar de equals() -->
        <module name="StringLiteralEquality"/>

    </module>

</module>
